<?php 
    require "../class01/dbConnect.php";
    // $db = get_db();

    // $events = $db->prepare("SELECT name, image FROM events");
    // $events->execute();

    // while ($eRow = $events->fetch(PDO::FETCH_ASSOC)) {
    //     $event_name = $eRow["name"];
    //     $event_img = $eRow["image"];

    //     echo "<div><p>$event_name</p><img src=\"$event_img\"/></div>";
    // }
?>